import React from 'react';
import Styled from 'styled-components/native';

const Container = Styled.ScrollView``;

const Done = () => {
    return(
        <Container>
        
        </Container>
    );
};
export default Done;